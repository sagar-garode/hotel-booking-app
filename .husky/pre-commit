#!/usr/bin/env sh
. "$(dirname "$0")/husky.sh"

echo "Running lint checks .."
npm run lint:all

RESULT=$?
if [ $RESULT -ne 0 ]; then
    echo "Eslint Failed !! : fix errors before commit"
    exit 1
fi

echo "Eslint passed !!"

echo "Running prettier checks .."
npm run format:fix